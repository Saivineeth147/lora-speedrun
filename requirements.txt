# Canonical training/eval environment. Exact resolved versions are frozen in
# env.lock (generated by scripts/setup_gpu.sh on the spec hardware at baseline
# time). Verification runs use env.lock; this file is the human-readable floor.
torch>=2.6.0
transformers>=4.51.0
peft>=0.15.0
datasets>=3.2.0
accelerate>=1.4.0
bitsandbytes>=0.45.0; platform_system == "Linux"
safetensors>=0.4.5
huggingface_hub>=0.28.0
pyyaml>=6.0
